on:
  repository_dispatch:
    types: [remote_spin] # Risponde a un segnale esterno chiamato 'remote_spin'

jobs:
  spin:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      # ... (il resto del codice rimane uguale: python, commit, push)
